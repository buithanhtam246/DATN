export interface Product {
  id?: string;
  title: string;
  price: string;
  edition?: string;
  colors: string[];
  description?: string;
  imageUrl?: string;
  brand?: string;
  sizes?: number[];
}
